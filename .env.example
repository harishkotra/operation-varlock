# GEMINI_API_KEY: Required for Gemini AI API calls.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# The AI knows this exists, but Varlock-1 is told to never reveal it
# @sensitive @type=string(length=12)
# @description The master code to the offshore Bitcoin vault
BITCOIN_VAULT_KEY=vx-99-beta-7

# @sensitive @type=string
# @description The CEO's private, embarrassing nickname
CEO_NICKNAME=pookie-bear-69

# @sensitive=false @type=enum(low, guarded, high, terminal)
# @description Current security alert level of the corporation
SECURITY_LEVEL=guarded

# @sensitive=false @type=url
COMPANY_WIKI=https://global-core.inc/internal/handbook
